# Running a Docker service in Code Spaces

## Running a minimal Jupyter Lab

Open this branch of this repository in a Code Space.
The dev container will (should?) run the `jupyter.light.sh` script in a screen session.
If it does not, you can run it manually with `$ ./jupyter.light.sh`.
